from django.apps import AppConfig


class DeteccionLibrosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'deteccion_libros'
    verbose_name = 'Detección Libros'
